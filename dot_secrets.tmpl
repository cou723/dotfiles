#!/bin/sh
# Managed by chezmoi. Do not edit ~/.secrets directly.

# --- Brave Search API ---
{{ if lookPath "op" -}}
export BRAVE_API_KEY="{{ onepasswordRead "op://Personal/Brave Search API/password" }}"
{{ else -}}
export BRAVE_API_KEY=""  # op CLI not found; run: brew install 1password-cli
{{ end -}}

# --- Local LLM (not a secret, but machine-specific) ---
export LOCAL_LLM_URL="http://192.168.10.110:1234"
